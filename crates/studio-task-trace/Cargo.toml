[package]
name = "studio-task-trace"
version = "0.1.0"
edition.workspace = true
license.workspace = true
[dependencies]
critical-section = "1"

[dev-dependencies]
critical-section = { version = "1", features = ["std"] }
